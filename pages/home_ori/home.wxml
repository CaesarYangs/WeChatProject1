<!--pages/home.wxml-->
<view wx:for="{{list}}">
  <view>res:连接成功</view>
  <view>id:{{item.id}}</view>
  <view>name:{{item.name}}</view>
  <view>task:{{item.taskname}}</view>
  <view>done? : {{item.done}}</view>
</view>

<view wx:for="{{list}}">
  <view>res:连接成功</view>
  <view>id:{{item.id}}</view>
  <view>name:{{item.name}}</view>
  <view>task:{{item.taskname}}</view>
  <view>done? : {{item.done}}</view>
</view>

<view class="page">
    <view class="page__bd">
        <view class="weui-btn-area">
            <button class="weui-btn" type="default" bindtap="openConfirm">确认取消按钮</button>
            <button class="weui-btn" type="default" bindtap="tapOneDialogButton">只有确认按钮</button>
        </view>
    </view>

    <mp-dialog title="test" show="{{dialogShow}}" bindbuttontap="tapDialogButton" buttons="{{buttons}}">
        <view>0</view>
    </mp-dialog>
    <mp-dialog title="test1" show="{{showOneButtonDialog}}" bindbuttontap="done" buttons="{{oneButton}}">
        <view>test content1</view>
    </mp-dialog>
</view>

<view><text class="usermotto">{{motto}}</text></view>
<button class="weui-btn" type="default" bindtap="changeText">btn测试</button>
<button class="weui-btn" type="default" bindtap="jumpTo">btn跳转</button>

<text>pages/home.wxml</text>
<text>helllllllllo</text>
