<view wx:if="{{nlist==''}}">

</view>
<view wx:else class="list-page">  
  <view class="marks" wx:for="{{marklist}}">
    <text class="markitem" bindtap="seeMark" data-markname="{{item}}">{{item}}</text>
  </view>
</view>
